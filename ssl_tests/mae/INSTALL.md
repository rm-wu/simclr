# Install MAE

This part of the repository requires having `dino_env` from `../dino/` installed.

```bash
conda create -n mae --clone dino_env
pip install timm==0.4.5 # later versions are not compatible
```


# DINO environment installation
I dumped into the `environment.yaml` file the dependencies I used to install the environment of SAMv2.

To install the environment, run:
```bash
conda env create -f environment.yaml
```

